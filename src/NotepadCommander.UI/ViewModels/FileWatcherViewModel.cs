namespace NotepadCommander.UI.ViewModels;

public class FileWatcherViewModel : ViewModelBase
{
    // FileWatcher events are already handled by TabManagerViewModel.
    // This VM exists as a placeholder for future file watcher UI state
    // (e.g., "watching N files" indicator, watcher enable/disable toggle).
}
